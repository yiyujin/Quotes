import NavLink from './nav-link';
// import { getBooks } from '../../lib/data';

export default async function BookLinks() {

  // const books = await getBooks();

  return (
    <>
      {/* {books.map((book) => (
        <NavLink
          key = {book.id}
          href = {`/books/${book.id}`}
          title = {book.title}
        />
      ))} */}
    </>
  );
}